defaults:
  - disease: ra
  - ml: defaults

pipeline:
  key_file: /home/ofekvi/.secrets/adc.json
  project: biomarkers-478606
  bq_dataset: biomarkers-478606.biomarkers
  dry_run: false

split_salt: ""  # set to e.g. "_seed2" to produce a labelled alternate split

index_date:
  index_window_hours: 24   # CBC from first N hours of index admission
  lookback_days: 90        # CBC from prior N days (different admission)
